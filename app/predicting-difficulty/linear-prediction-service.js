angular.module('ethMiningCalc')
  .factory('linearPrediction', [function() {
    var factory = {};

    // Linear Prediction of Difficulty 
    factory.predict = function(difficultyData) 
    {
    };

    return factory;
  }]);
